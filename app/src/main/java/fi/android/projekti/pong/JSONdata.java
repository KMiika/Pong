package fi.android.projekti.pong;

import java.util.List;

public interface JSONdata {
/*
    //haettaisiin JSON array meidän databasesta
    @GET("HighScore")
    Call<List<data>> haeData();

    main activityyn tehtäisiin placeholderi  ja JSON builderi tyyliin
    jsonbuild jsonbuild = new jsonbuild.Builder()
        .baseURL("https://meidänJSONDATA.com")
        .addConverterFactory(GsonConverterFactory.create())
        .build();

    JsonPlaceHolderApi jsonPlaceHolderApi = new jsonbuild.create(JsonPlaceHolderApi.class)

    Call<List<HighScore>> call = jsonPlaceHolderApi.getHighScore();

    call.enqueue(new Callback<List<Post>>(){
        @Override
        public void onResponse(Call<List<HighScore>> call, Response<List<HighScore>> response){

        HighScorej.java sisältö, myös pitäisi koodiin lisätä vertailu jos tämänhetkinen pisteytys > highscore niin päivitetään se Databaseen
        myös pitää permissionit muistaa laittaa manifest.xml kerta otetaan yhteys internettiin...
        }
        @Override
        public void onFailure(Call<List>HighScore>> call, Throwable t) {

        }
        });
    */

}
